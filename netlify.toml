[build]
  command = "npm run build"
  publish = "dist"

# Preserve the existing public domain while Fastify serves the application.
[[redirects]]
  from = "/*"
  to = "https://nexo-governamental-production.up.railway.app/:splat"
  status = 200
  force = true
